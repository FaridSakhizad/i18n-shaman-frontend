# i18nshaman app

Vite-based frontend for i18nshaman.

## Environment

Create `.env` from `.env.example` and set:

```env
VITE_API_URL=http://localhost:4000
```

## Scripts

```sh
npm run dev
npm run build
npm run lint
npm run preview
```
